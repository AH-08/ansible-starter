---

- name: Check STCs
  hosts: zos
  gather_facts: false
  environment: "{{ environment_vars }}"

  tasks:
  - name: Query a job with a job name of '*'
    ibm.ibm_zos_core.zos_job_query:
       job_name: "*"